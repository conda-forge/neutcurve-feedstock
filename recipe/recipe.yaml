schema_version: 1

context:
  version: "2.4.0"

package:
  name: neutcurve
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/n/neutcurve/neutcurve-${{ version }}.tar.gz
  sha256: b52288683f1fa29e42d9a2281ade86977930e2ce735be8cbe7b414914e9b518b

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  host:
    - python =${{ python_min }}
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - dmslogo >=0.2.1
    - matplotlib-base >=3.0.0
    - openpyxl >=3.0
    - pandas >=1.0.1
    - pyyaml >=3.13
    - scipy >=1.1.0
    - xlrd >=1.2

tests:
  - python:
      imports:
        - neutcurve
  - requirements:
      run:
        - pip
        - python =${{ python_min }}
    script:
      - pip check

about:
  summary: Plot and fit neutralization curves.
  license: GPL-3.0-or-later
  license_file: LICENSE.txt
  homepage: https://github.com/jbloomlab/neutcurve

extra:
  recipe-maintainers:
    - danielnachun
